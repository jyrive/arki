{
	"version": 2,
	"buildCommand": "pnpm build",
	"outputDirectory": "build/client",
	"functions": {
		"api/index.js": {
			"includeFiles": "build/**"
		}
	},
	"rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
